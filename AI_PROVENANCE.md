# AI 制作过程说明

本项目的部分角色动画图像由 ChatGPT/OpenAI 图像工具辅助生成或修改，随后进行了人工工作，包括：

- 角色一致性选择与淘汰；
- 动作行拆分、方向校准和帧注册；
- 透明背景、固定网格和空单元检查；
- 动画连贯性、失败帧和方向语义审查；
- 无损精灵表打包与自动化验证。

相关提示词和制作期元数据保留在 `source/prompts/` 与 `metadata/`，质量检查记录位于 `qa/`。

最终精灵表不是模型输出后未经检查直接发布。桌面/CLI WebP 是完整 PNG atlas 的逐像素无损转换；ChatGPT Web 版本是同一 atlas 前 9 个标准状态行的逐像素无损裁切与转换。构建脚本会重新读取产物并比对 RGBA 像素。

AI 参与制作并不表示模型或 OpenAI 持有本项目版权，也不会自动消除输入素材、参考角色和第三方作品的权利。本项目依据维护者取得的非商业许可发布，并明确标注为非 OpenAI 官方项目。
